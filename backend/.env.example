# Banco local (PostgreSQL)
# Exemplo: Docker: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=zombie_td postgres:16
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zombie_td

# Para produção (Neon Tech): use a connection string do painel Neon
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

JWT_SECRET=altere-isso-em-producao
PORT=3000
